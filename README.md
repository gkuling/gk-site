# Grey Kuling — Personal Site (Jekyll + Just the Docs)

This repo hosts a lightweight professional site using the **Just the Docs** Jekyll theme.

## Local preview
1) Install Ruby (3.3+ recommended), then:
```bash
bundle install
bundle exec jekyll serve